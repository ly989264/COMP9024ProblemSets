#pragma once
#include <stdio.h>
#define PROBLEM_1_4

// Write a C function that takes a positive integer n as argument and outputs a series of
// numbers according to the following process, until 1 is reached :
// • If n is even, set n to n / 2
// • If n is odd, set n to 3 * n + 1

void operate(int n);